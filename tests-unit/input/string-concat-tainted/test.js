const input = "the user's credit card number";

function sendToThirdParty(data) {
    const message = "the user's credit card number is " + data;
    const output = message;
}

sendToThirdParty(input);

