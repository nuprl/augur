'use strict';
var execSync = require('child_process').execSync;

module.exports = function () {
  var output = execSync('sysctl -n kern.boottime').toString();
  var date = output.replace(/{[ ,=\w]+} /g, '').trim();

  return new Date(date);
};
