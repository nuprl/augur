{
  "name": "js-taint-analysis",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "build:watch": "./node_modules/.bin/tsc --watch",
    "test": "NODE_PATH=. && jest",
    "test:generate": "node dist/generateTests.js"
  },
  "dependencies": {
    "@types/node": "10",
    "tslint": "^5.11.0",
    "typescript": "^3.1.5"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.5.0",
    "ts-jest": "^24.0.0"
  }
}
